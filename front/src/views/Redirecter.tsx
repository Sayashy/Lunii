import { Fragment } from 'react';
import RedirecterComponent from '../components/Redirecter';

const Redirecter = () => {
  return (
    <Fragment>
      <RedirecterComponent />
    </Fragment>
  );
};

export default Redirecter;
