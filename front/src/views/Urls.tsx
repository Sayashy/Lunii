import { Fragment } from 'react';
import UrlsComponent from '../components/Urls';

const Home = () => {
  return (
    <Fragment>
      <UrlsComponent />
    </Fragment>
  );
};

export default Home;
